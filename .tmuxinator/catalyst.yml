# ~/.tmuxinator/catalyst.yml

name: catalyst
# This stopped working after tmux upgrade, use 'pre' instead
# root: ~/github/ab

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# Sets current postgres version and cluster (so all pg_related helper apps versions match psql server app (like pg_dump, pg_restore))
# Strings separated by space are respectively: postgres version, host:port, database (* = anything)
pre: |
  cd ~/github/catalyst/client
  export DISABLE_AUTO_TITLE=true

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
pre_window: |
  export DISABLE_SPRING=1
  rvm use 2.5.0
  nvm use 8.9.1
  export CURRENT_PROJECT_NAME='CATALYST'
  export VIMUX_RUNNER_PANE_INDEX='3'
  tmux source ~/.tmux.conf

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: tmux-next

# Specifies (by name or index) which window will be selected on project startup. If not set, the first window is used.
startup_window: editor
startup_pane: 0

# Controls whether the tmux session should be attached to automatically. Defaults to true.
# attach: false

# Runs after everything. Use it to attach to tmux with custom options etc.
# post: tmux -CC attach -t ab

windows:
    - frontend:
        layout: 4ecf,283x58,0,0{65x58,0,0[65x14,0,0,14,65x43,0,15,20],217x58,66,0[217x10,66,0,16,217x47,66,11,17]}
        panes:
            - build_watcher:
                - cat_webpack_build_watcher.sh
            - server:
                - npm i
                - npm start
            - helper:
                - open https://chicago.catalystmeeting.dev
                - clear
                - gst
            - editor:
    - backend:
        layout: 3cad,254x51,0,0{58x51,0,0,6,195x51,59,0[195x13,59,0,7,195x37,59,14,8]}
        panes:
            - server:
                - cd ..
                - dcd up -d && dcd logs -f
            - helper:
                - cd ..
                - clear
                - gst
            - editor:
                - cd ..
